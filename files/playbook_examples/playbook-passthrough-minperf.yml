---

# The minperf playbook stands for "minimal performance".
# The Windows VM is on an image file with no disk dedicated to the VM. It gives decent performance.
# Please take your time to adjust the size for "lsw_windows_img_size" you can give to your image depending.
# on the size you have on your only disk.
# Bluetooth controller is passthroughed to the VM.
# Note: Looking Glass musts be installed because no 2nd keyboard and mouse are set.
# Adjust the variables with files/documentation/VARIABLES.md.

- name: A man needs a name
  hosts: localhost
  connection: local
  gather_facts: true
  become: false
  vars:
    lsw_windows_iso: windows11.iso
    lsw_windows_version: 11
    lsw_windows_img_size: 100G
    lsw_windows_gpu_driver: nvidia
    lsw_passthrough_gpu_pci_base_addr: 01:00
    lsw_install_looking_glass: true
    # lsw_config_is_laptop: true
    lsw_config_add_bluetooth: true
    # lsw_config_bluetooth_address: 8087:0033
  roles:
    - ansible-role-lsw
